
global:
  allowedIngressHost: "{{ .Values.allowedHosts[0] }}"
  allowedHosts:
    host: "{{ .Values.allowedHosts[0] }}"

  captchaKey: "{{ .Values.captchaKey }}-test"


postgresql:
  enabled: true

redis:
  enabled: true

nfs-server-provisioner:
  enabled: true

saleor-core:
  enabled: true

saleor-storefront:
  enabled: true

saleor-dashboard:
  enabled: true
